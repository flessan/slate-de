[package]
name = "slate-vision"
version.workspace = true
edition.workspace = true

[dependencies]
slate-core = { path = "../core" }
anyhow = { workspace = true }
async-trait = "0.1"
serde = { workspace = true, features = ["derive"] }
